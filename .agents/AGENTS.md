# Project Rules

## Git & Version Control
- **NO AUTOMATIC COMMITS/PUSHES**: NEVER execute `git add`, `git commit`, or `git push` commands.
- Only make changes to the local file storage on the computer.
- The user will review all modifications and handle version control (staging, committing, and pushing) manually at the end of their sessions to avoid commit pollution.
